Install Pandas and Numpy:
pip install pandas
pip install numpy

The two libraries will help with manipulatin the data

Before running install the virtual environment:
pip install pipenv

To run the program run:
"pipenv shell" to activate the virtual environment

Enter python3 main.py to run

Cheers
Happy coding!

